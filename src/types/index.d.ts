export type FormInputPost = {
    title : string,
    content: string,
    tagId: string
}
export type FormChangeLogPost = {
    title : string,
    description: string,
    releaseVersion: string,
    releaseCategory: string
}